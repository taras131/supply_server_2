version: '3.8'
services:
  web:
    build: .
    ports:
      - "443:443"
    volumes:
      - ./ssl:/ssl
    environment:
      - SSL_KEYFILE=/ssl/certificate.key
      - SSL_CERTFILE=/ssl/certificate